<div class="container">
    <table class="table">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Nombre</th>
                <th scope="col">Correo</th>
                <th scope="col">Regular</th>
                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            {{#if students }}
                {{#each students}}
                    <tr>
                        <th scope="row">{{@key}}</th>
                        <td>{{this.name}}</td>
                        <td>{{this.email}}</td>
                        <td>
                            {{#if this.isRegular}}
                                Sí
                                {{^}}
                                No
                            {{/if}}
                        </td>
                        <td>
                            <form method="POST" action="/user/{{this._id}}?_method=DELETE">
                                <button type="submit" class="btn btn-danger">Eliminar</button>
                            </form>
                        </td>
                    </tr>
                </td>
            </tr>
        </tbody>
    {{/each}}
{{/if}}</tbody></table></div>
