server {
    listen 80;
    server_name nms.zequenze.com nms.ops.zequenze.com nms01.ops.zequenze.com 172.16.10.2
    server_tokens off;

    client_max_body_size 4G;

    access_log /var/log/nginx/icinga2-web-access.log main;
    error_log /var/log/nginx/icinga2-web-error.log;

    expires $expires;

    location /nginx_status {
        stub_status on;
        access_log off;
        allow 127.0.0.1;
        deny all;
    }

    location / {
      proxy_set_header Host $http_host;
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Server $host;
      proxy_pass http://grafana.server:3000/;
    }

}
